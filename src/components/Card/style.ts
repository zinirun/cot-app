import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    bgContainer: {
        width: 200,
        height: 100,
        borderRadius: 12,
    },
    container: {
        padding: 12,
        width: '100%',
        height: '100%',
    },
});
