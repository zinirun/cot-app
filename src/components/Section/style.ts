import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    sectionContainer: { backgroundColor: 'black', height: '100%' },
    contentContainer: {
        padding: 12,
    },
});
